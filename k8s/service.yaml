apiVersion: v1
kind: Service
metadata:
  name: bond-portfolio-service
  namespace: bond-portfolio
  labels:
    app: bond-portfolio-analyzer
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8501
      protocol: TCP
  selector:
    app: bond-portfolio-analyzer